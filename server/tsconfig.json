{
  "compilerOptions": {
    // ... (otras opciones)

    "module": "commonjs", // O "esnext" si usas módulos ES
    "moduleResolution": "node", //  <- Agrega esta línea

    "esModuleInterop": true,
    "strict": true,
    "skipLibCheck": true, // <- Cambia a false (temporalmente)

    "outDir": "./dist",
    "target": "es2016"

    // ... (otras opciones)
  },
  "include": [
    "src/**/*.ts",
    "index.ts"
  ],
  "exclude": [
    "node_modules"
  ]
}